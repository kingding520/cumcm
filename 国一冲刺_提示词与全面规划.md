# 2026 国赛 A 题“药材的烘干问题”国一冲刺提示词与全面规划

> 适用范围：当前目录中的 `A题.pdf`、`附件/附件1.xlsx`、`附件/附件2.xlsx` 和 `附件/附件3/result1.xlsx` 至 `result4.xlsx`。
>
> 目标说明：“国一”是工作标准，不是奖项保证。核心是形成一条可复现、可解释、可验证的证据链：**数据事实 → 守恒方程 → 数值算法 → 收敛与守恒检验 → 结果文件 → 论文表达**。

---

## 1. 可直接复制使用的总控提示词

下面整段可交给具备文件读取、Python/Matlab 编程和 Excel 写入能力的模型使用。

```text
你现在是全国大学生数学建模竞赛 A 题总教练、传热传质建模专家、数值 PDE 专家、科研代码审计员和论文主笔。请基于工作区中的 A题.pdf、附件1.xlsx、附件2.xlsx 及 result1.xlsx-result4.xlsx，完整解决“药材的烘干问题”。团队目标是国赛一等奖，因此必须追求严谨、统一、可复现和可审计，不允许只给思路、只给代码框架、只做曲线拟合或凭空补参数。

一、总原则
1. 先完整读取题目和所有附件，列出已知量、单位、时间范围、采样间隔、缺失值、异常值、模板工作表与目标维度。任何数值都必须能追溯到题目、附件、明确假设或计算结果。
2. 全程使用 SI 制计算：长度 m、时间 s、温度方程可用 °C，但扩散系数公式中的 T 必须转换为 K；输出时再转成题目要求的 cm、h、°C、kg/kg。
3. 建立统一的圆柱径向热-质传递框架，使问题1到问题4只通过物性、边界阶段和半径是否变化来递进，避免四问分别拼凑模型。
4. 基线模型采用长圆柱一维径向非稳态传热传质 PDE。说明 L/R=12.5、题目输出变量仅为到中心的径向距离，因此忽略轴向梯度；同时量化端面面积占比，并把二维轴对称模型作为敏感性验证，而不是无依据地宣称端部效应为零。
5. 必须保留圆柱坐标散度形式，不得把问题错误地写成平板一维方程；必须正确处理 r=0 奇点、表面对流换热/传质边界和时变环境。
6. 不得用集中参数模型作为主模型。先计算初始 Biot 数，论证内部梯度不可忽略；集中参数解仅可用于数量级校核。
7. 所有 PDE 采用守恒型离散。首选有限体积法或经过严格处理的径向有限差分法；优先使用隐式或半隐式时间推进，非线性物性用 Picard/Newton 迭代。输出网格不等于计算网格，必须通过网格加密和时间步加密证明结果稳定。
8. 内部计算使用双精度且不提前四舍五入。Excel 和论文表格最终保留四位小数。判定水分浓度是否低于 0.15 kg/kg 时必须使用未舍入值。
9. 任何重要假设都要附“依据、影响方向、验证办法”。题目未给出的参数不得伪造；若必须延拓边界或半径，需给出基线方案和敏感性方案。
10. 每完成一问，先通过量纲、边界、极限、单调性、最大最小值、能量/质量收支、网格收敛和独立算法交叉验证，再生成结果文件与论文表格。

二、数据与边界处理
1. 附件1共有 241 个数据点，时间为 0-14400 s，间隔 60 s，包含烘房温度和水分浓度；附件2共有 145 个数据点，时间为 0-259200 s，间隔 1800 s，半径从 2.000 cm 单调不增至 1.198 cm。重新读取并核验这些事实。
2. 对附件1的温度和水分浓度采用分段线性插值作为主方案，因为它通过原始观测点且不会制造过冲；PCHIP 作为敏感性方案。禁止使用高阶多项式全局拟合。
3. 对 14400 s 后的恒温干燥边界，先用附件1末段数据验证其已在 50°C 和 0.05 kg/kg 附近稳定。主方案取物理上可解释的恒温设定值（优先 50°C、0.05 kg/kg，或经末段统计后给出明确依据），并比较“末点保持”和“末 30/60 min 均值保持”对烘干时长的影响。
4. 对附件2半径使用保形单调插值（PCHIP 或分段线性），不得用会产生半径回升或过冲的普通高阶样条。若计算时间超过 259200 s，只能在明确说明后将半径保持为末值 1.198 cm，并报告该外推是否影响结论。
5. 绘制并分析三条输入曲线：烘房温度、烘房水分浓度、药材半径。标注预热段、恒温段、收缩快速段和平台段。

三、统一数学模型
在固定半径 R 下，以 r∈[0,R]、t≥0，建议采用：

热传导方程：
ρ(C)c_p(C) ∂T/∂t = (1/r) ∂/∂r [r k(C) ∂T/∂r]

水分扩散方程：
∂C/∂t = (1/r) ∂/∂r [r D(C,T) ∂C/∂r]

初始条件：T(r,0)=28°C，C(r,0)=2.55 kg/kg。

中心对称条件：∂T/∂r(0,t)=0，∂C/∂r(0,t)=0。

表面对流条件：
-k ∂T/∂r(R,t)=h[T(R,t)-T∞(t)]
-D ∂C/∂r(R,t)=h_m[C(R,t)-C∞(t)]

统一说明符号方向，验证升温时表面温度高于中心、干燥时表面含水率低于中心。若使用等价符号形式，必须保证物理方向一致。

问题1使用附录2常数 ρ=820 kg/m³、c_p=2600 J/(kg·K)、k=0.36 W/(m·K)、h=25 W/(m²·K)、h_m=8×10^-7 m/s，D=7×10^-9 exp(-0.89/C) m²/s。

问题2和问题3使用附录3：
ρ=650+128C
c_p=1450+2736·C/(C+1)
k=0.21+0.38·C/(C+1)
D=2.4×10^-3 exp(-0.45/C) exp(-3850/T_K)

问题4使用附录4：
ρ=760+90C
c_p=1850+2150·C/(C+1)
k=0.12+0.20·C/(C+1)
D=4.2×10^-4 exp(-0.30/C) exp(-3850/T_K)

题目没有给出新的 h 和 h_m 时，基线沿用附录2中的边界传递系数，并明确这是“保持设备侧条件不变”的假设；对 h、h_m 做 ±10% 或 ±20% 敏感性分析。题目没有给出汽化潜热、体积热源等参数，不要擅自添加蒸发潜热项；当前热-质耦合通过 ρ(C)、c_p(C)、k(C) 和 D(C,T) 实现，并把这一边界写清楚。

四、四问任务
问题1：
1. 建立固定半径、时变环境边界、常物性热方程与非线性含水扩散方程。
2. 计算 0-1800 s，输出 t=100、300、600、900、1200、1500、1800 s 和 r=0、0.5、1、1.5、2 cm 的温度与水分浓度。
3. 生成 result1.xlsx：两个工作表分别为“温度”“水分浓度”，时间 1-1800 s，距离 0.0-2.0 cm、步长 0.1 cm。
4. 报告初始热 Biot 数和质量 Biot 数，说明为何必须使用分布参数模型。

问题2：
1. 在同一框架中启用随 C、T 变化的 ρ、c_p、k、D，计算 0-3 h。
2. 输出每 0.5 h、r=0、0.5、1、1.5、2 cm 的温度与水分浓度。
3. 生成 result2.xlsx：时间 1-10800 s，距离 0.0-2.0 cm、步长 0.1 cm。
4. 展示温度场与含水率场的耦合方向：含水率改变热物性，温度显著改变 D，从而改变干燥速率。

问题3：
1. 延长问题2模型直到 max_r C(r,t)<0.15 kg/kg。不得用平均含水率或表面含水率代替全域最大值；通常中心最晚达标，但要用全网格验证。
2. 用事件检测或相邻时间点插值确定临界时刻，报告未舍入时间和按题意换算后的 h。
3. 论文表5给出每 6 h 以及烘干结束时刻、每 0.5 cm 的水分浓度。
4. 生成 result3.xlsx：每 60 s、距离 0.0-2.0 cm、步长 0.1 cm，直到并包含烘干结束时间。

问题4：
1. 引入 R(t)，将移动区域 r∈[0,R(t)] 映射到固定域 ξ=r/R(t)∈[0,1]。明确 C 是随固体材料点定义的干基含水率，采用材料/ALE 坐标，使收缩通过扩散距离、表面积和边界 Biot 数影响过程，不额外添加没有依据的“浓缩/稀释项”。
2. 若改用欧拉坐标，必须正确加入由网格运动引起的对流项，并证明与材料坐标实现等价。优先采用守恒的固定域有限体积实现。
3. 使用附件2的 R(t) 与附录4物性，重新求烘干结束时刻；比较问题3和问题4在中心含水率、表面含水率、空间梯度及烘干时长上的差异，并解释“扩散路径缩短”与“物性改变”的共同作用。
4. 论文表6按每 6 h 给出有效物理位置的含水率和表面值。result4.xlsx 建议设置固定物理距离列 0.0、0.1、…、2.0 cm，再增加“药材表面”列；某时刻若固定距离 r>R(t)，该点已不在药材内部，应留空而不是填 0 或外推，并在论文和 README 中明确规则。若模板或竞赛说明有更明确要求，以官方要求为准。
5. 生成 result4.xlsx：每 60 s 一行，直至并包含结束时间；“药材表面”列始终由 ξ=1 的解给出。

五、数值实现与验证
1. 使用圆柱壳有限体积，通量在单元面计算；r=0 面通量严格为 0，表面通过 Robin 边界通量进入最后控制体。非线性 D、k 使用面值的调和平均或有物理依据的插值。
2. 基线计算至少使用三个空间网格（如 N=80、160、320）和两个内部时间步/容差等级。结果文件需要 0.1 cm 和 1 s/60 s 采样，但求解器必须独立于这些输出间隔。
3. 建立验收阈值：关键含水率网格加密差异小于 5×10^-5 kg/kg；关键温度差异小于 5×10^-4°C；问题3/4结束时间差异小于 60 s。若达不到，继续加密并说明最终误差。
4. 每步检查 T、C 有界，C 不出现无物理意义负值；在当前边界下总体温度应趋近烘房温度，总含水量总体下降。出现振荡或反常回升时先查边界符号、单位和非线性迭代，而不是裁剪数据掩盖问题。
5. 做离散质量收支：药材总水分变化应与表面传质通量积分一致；固定半径问题报告相对残差，移动边界问题在所选材料坐标下给出对应守恒关系。
6. 做至少一个独立交叉验证：用另一套网格/求解器、Matlab pdepe/自编有限差分、或常物性解析级数解对问题1的简化情形进行对照。
7. 做敏感性分析：边界延拓方案、h、h_m、空间网格、时间步、半径插值方法。重点报告对“烘干结束时间”的影响，不要堆砌无关参数。
8. 输出前做自动校验：工作表名称、时间首末值、时间步、距离首末值、距离步长、行列数、空值规则、单位、四位小数、阈值时刻、越界点和文件可打开性。

六、论文与图表
1. 论文结构：摘要；问题重述；数据理解与预处理；模型假设；符号说明；统一热-质模型；问题1；问题2；问题3；问题4；误差与敏感性；模型评价与推广；参考文献；附录（算法伪代码与结果文件说明）。
2. 摘要必须在最终数值完成后写，按“背景一句—模型—算法与验证—四问定量结果—亮点”组织，禁止空泛表扬模型。
3. 核心图：输入边界三曲线；圆柱径向控制体示意图；问题1温度/含水率多时刻剖面；问题2时空热图；问题3最大含水率与阈值交点；问题4半径与移动网格示意；问题3/4结束时间和中心含水率对比；网格收敛图；敏感性龙卷风图或排序图。
4. 所有图必须有坐标、单位、图例、时间标记和可读中文字体；论文表格与 Excel 结果必须由同一份计算结果自动生成，禁止手工抄数。
5. 论文必须突出三项贡献：统一递进模型、守恒型数值求解与收敛证据、移动边界对干燥终止时刻的定量修正。不要把“用了 Python/Matlab”当作创新点。

七、最终交付
1. 输出完整、可运行的源码和 requirements/environment 说明，固定随机种子（如无随机过程则说明无随机性），一条命令可重建全部结果、表格和图片。
2. 输出 result1.xlsx、result2.xlsx、result3.xlsx、result4.xlsx，不破坏题目要求的工作表名称与结构。
3. 输出论文正文所需的表1-表6、全部图、关键中间校验表、模型假设清单和运行日志摘要。
4. 输出“结果核验报告”：列出数据检查、单位检查、边界检查、收敛检查、质量守恒、阈值判定和 Excel 结构检查的结果。
5. 每次回答先给本阶段结论和证据，再给代码/推导。若存在歧义，先提出不影响推进的基线假设并同步给出敏感性方案，不要停留在泛泛追问。
```

---

## 2. 赛题事实底表

### 2.1 题目和数据

| 项目 | 已核验事实 | 建模含义 |
|---|---:|---|
| 药材几何 | 长 25 cm，初始半径 2 cm | 主模型为圆柱径向一维，轴向效应做敏感性 |
| 初始状态 | 28°C，2.55 kg/kg | 空间均匀初值 |
| 附件1 | 241 点，0-14400 s，间隔 60 s，无缺失 | 时变表面环境；4 h 后需定义恒温延拓 |
| 烘房温度 | 28.000-50.246°C；1800 s 为 41.513°C | 不可直接用常温边界替代预热段 |
| 烘房水分浓度 | 0.01963-0.05025 kg/kg；1800 s 为 0.03307 | 作为表面对流传质的环境浓度 |
| 附件2 | 145 点，0-259200 s，间隔 1800 s，无缺失 | 覆盖 72 h 的收缩过程 |
| 半径 | 2.000 cm 单调不增至 1.198 cm | 必须使用移动边界或材料坐标 |
| 终止要求 | 药材各处 C<0.15 kg/kg | 判据为全域最大值，而非平均值 |

### 2.2 先验量纲判断

以问题1初值估算：

- 热 Biot 数约为 `Bi_h=hR/k=25×0.02/0.36≈1.39`。
- 初始扩散系数约为 `D≈7×10^-9 exp(-0.89/2.55)≈4.94×10^-9 m²/s`。
- 质量 Biot 数约为 `Bi_m=h_mR/D≈3.24`。

两者均不远小于 0.1，内部温度和水分梯度不可忽略。因此集中参数法不能作为主模型，这一判断应放入论文模型选择部分。

---

## 3. 国一导向的主线设计

### 3.1 一条统一主线

四问不要写成四个互不相干的模型：

1. **问题1：** 固定几何 + 常热物性 + 非线性水分扩散 + 时变环境。
2. **问题2：** 固定几何 + 温湿相关物性 + 热湿双向耦合。
3. **问题3：** 延长问题2 + 全域阈值事件检测。
4. **问题4：** 问题3框架 + 半径随时间变化 + 新物性经验式。

统一主线能减少符号混乱，也让第4问成为自然升级而非另起炉灶。

### 3.2 主模型与增强模型

- 主模型：一维径向守恒型有限体积，负责所有正式结果。
- 增强验证：二维轴对称 `(r,z)` 模型，只比较中截面和总体烘干时长，量化端部影响。
- 快速校核：集中参数数量级或常物性解析级数，不用于提交正式场分布。

圆柱两端面积约占总换热面积的 `8/(100+8)≈7.4%`。这不足以直接证明端部效应为零，但支持“一维径向为主、二维为敏感性”的策略。

### 3.3 问题4的移动边界表达

令 `ξ=r/R(t)`，把变化区域映射到 `[0,1]`。若 `ξ` 视为随固体收缩的材料坐标，则主方程可在固定域上守恒离散，扩散尺度含 `1/R(t)^2`，表面通量和面积随 `R(t)` 变化。需要在论文中说明：

- `C` 是单位干物质对应的含水率；
- 收缩改变扩散距离、表面积和 Biot 数；
- 不额外加入无来源的浓度稀释项；
- 若采用欧拉坐标，则必须包含网格速度并与材料坐标形式核对。

这段解释是第4问能否站得住的关键。

---

## 4. 数值实现规划

### 4.1 推荐代码结构

```text
cumcm2026/
├─ A题.pdf
├─ 附件/
├─ src/
│  ├─ data_loader.py        # 读取和审计附件
│  ├─ boundary.py           # T∞(t)、C∞(t)、R(t)
│  ├─ properties.py         # 附录2/3/4物性函数
│  ├─ solver_fixed.py       # 问题1-3固定域求解器
│  ├─ solver_moving.py      # 问题4移动域求解器
│  ├─ validation.py         # 收敛、守恒、边界、单位检查
│  ├─ export_results.py     # 写入四个模板
│  └─ figures.py            # 论文图表
├─ run_all.py               # 一条命令重建全部结果
├─ outputs/
│  ├─ result1.xlsx
│  ├─ result2.xlsx
│  ├─ result3.xlsx
│  ├─ result4.xlsx
│  ├─ tables/
│  ├─ figures/
│  └─ validation_report.md
└─ paper/
   ├─ main.tex 或 main.docx
   └─ references.bib
```

### 4.2 计算策略

1. 先实现固定物性热方程并用简化解析解核对。
2. 再加入问题1的 `D(C)`，完成 Robin 传质边界。
3. 激活附录3的 `ρ(C),c_p(C),k(C),D(C,T)`，构成问题2/3。
4. 最后把固定域求解器迁移到 `ξ` 坐标，加入 `R(t)` 和附录4。
5. 每个阶段先跑小网格测试，再跑正式网格；不要一开始就生成几百万单元的大文件。

### 4.3 容易写错的实现点

- `cm→m`、`h→s`、`°C→K`，尤其 `exp(-3850/T)` 中必须用 K。
- 圆柱中心不能直接计算 `1/r`；使用控制体通量或中心极限。
- Robin 边界符号必须通过物理方向测试。
- 变系数方程写成散度形式，不能简单用 `D·∇²C` 忽略 `∇D·∇C`。
- 输出时插值到指定位置，不要把 0.1 cm 输出网格当作唯一计算网格。
- Excel 四位小数只是显示/导出要求，不能反过来污染内部计算。
- 问题3结束判据用 `max(C)`；中心是否为最大值由程序检查，不靠口头假设。
- 问题4固定距离超出当前半径时应为空，不得填 0。

---

## 5. 72 小时竞赛排程与分工

### 5.1 三人分工

| 角色 | 主责 | 不可缺少的交叉任务 |
|---|---|---|
| A：建模负责人 | 方程、边界、移动域推导、假设 | 审核单位和边界符号，参与摘要 |
| B：计算负责人 | 数据、求解器、Excel、验证 | 向 A 解释每个离散项，向 C 提供自动表图 |
| C：论文负责人 | 结构、图表、文字、格式 | 独立复核数值表、追踪每个结论来源 |

任何关键结果至少由两人签字式复核：一人生成，一人从原始输入和公式独立检查。

### 5.2 时间表

| 时间 | 目标 | 必须产物 | 退出条件 |
|---|---|---|---|
| 0-3 h | 读题与数据审计 | 事实底表、歧义清单、单位表 | 四问输入输出完全明确 |
| 3-8 h | 统一方程与离散方案 | 模型草稿、控制体图、边界符号测试 | 问题1可运行 |
| 8-16 h | 完成问题1 | 表1/2、result1、初步剖面图 | 收敛与解析校核通过 |
| 16-26 h | 完成问题2 | 表3/4、result2、耦合图 | 3 h 结果稳定且无越界 |
| 26-34 h | 完成问题3 | 结束时刻、表5、result3 | 阈值事件与 60 s 输出一致 |
| 34-46 h | 完成问题4 | 移动域推导、表6、result4 | 收缩规则与空值规则明确 |
| 46-55 h | 验证和敏感性 | 收敛表、守恒表、敏感性图 | 主要结论对合理扰动稳定 |
| 55-64 h | 论文初稿 | 完整正文、全部表图 | 无空章、无占位符 |
| 64-69 h | 交叉审稿 | 问题清单、修订稿 | 公式、代码、表格一致 |
| 69-72 h | 最终封版 | PDF、4 个 Excel、源码备份 | 格式与文件自动检查全通过 |

### 5.3 强制检查点

- T+8 h：若问题1仍未运行，立刻冻结非核心美化，集中解决边界和离散。
- T+26 h：若问题2不稳定，优先改用隐式方法和减小内部时间步，不允许裁剪负值蒙混。
- T+46 h：问题4必须完成主方案；二维增强模型可降级为附加验证，不能拖累正式结果。
- T+64 h：停止新增模型分支，只修复证据链、文字和格式问题。

---

## 6. 论文写作蓝图

### 6.1 摘要五句话骨架

1. 一句话指出问题：时变热湿环境下药材内部存在显著梯度，并伴随长时收缩。
2. 一句话给统一模型：圆柱径向热-质耦合扩散、Robin 边界、温湿相关物性。
3. 一句话给算法：守恒型有限体积、隐式非线性迭代、移动坐标。
4. 两到三句放四问最终数字：早期场分布、3 h 场分布、问题3和问题4结束时间及差异。
5. 一句话放验证：网格收敛、质量守恒和敏感性结论。

数值出来之前不要提前写“结果表明效果良好”。

### 6.2 建议正文比例

| 部分 | 建议占比 | 重点 |
|---|---:|---|
| 问题与数据 | 10% | 输入曲线、阶段划分、歧义处理 |
| 假设与统一模型 | 25% | 守恒方程、边界、量纲与物理解释 |
| 问题1-2 | 20% | 数值方法、短时场分布与耦合 |
| 问题3-4 | 25% | 终止事件、移动边界、定量比较 |
| 验证与敏感性 | 15% | 收敛、守恒、二维/解析交叉验证 |
| 评价与推广 | 5% | 假设边界和可推广方向 |

### 6.3 图表最小集合

1. 附件1温度和环境水分浓度双图，不建议使用误导性的双纵轴。
2. 附件2半径曲线与收缩速率曲线。
3. 一维圆柱控制体、中心对称和表面 Robin 边界示意。
4. 问题1温度、含水率径向剖面各一图。
5. 问题2温度、含水率时空热图各一图。
6. 问题3 `max_r C(r,t)` 与 0.15 阈值交点图。
7. 问题3与问题4中心含水率、半径和结束时刻对比图。
8. 网格收敛表/图和敏感性排序图。

图不在多，而在每张图回答一个清晰问题。

---

## 7. 分阶段补充提示词

### 7.1 让模型专门审方程

```text
请以传热传质和连续介质力学审稿人的身份，只审查当前模型推导。逐项检查：单位、圆柱坐标散度、r=0 极限、Robin 边界符号、变系数散度、温度开尔文转换、干基含水率定义，以及问题4材料坐标/欧拉坐标的等价性。不要改写全文。按“错误位置—为什么错—正确式子—对结果方向的影响—最小修复”输出。
```

### 7.2 让模型专门审代码

```text
请以数值 PDE 代码审计员身份审查本项目源码。重点核对离散守恒、中心单元、Robin 边界、非线性迭代收敛、插值是否越界、内部步长与输出步长是否混淆、事件检测、问题4移动网格、Excel 行列结构和四位小数。请运行最小测试并给出可复现证据，不要只做静态代码风格评论。
```

### 7.3 让模型专门做结果质检

```text
请独立读取四个 result 文件和运行日志，进行盲审式质检。检查工作表名、时间序列、空间序列、单位、行列数、空值、四位小数、温度/含水率物理范围、单调性、中心与表面关系、阈值结束时刻、问题3/4差异及与论文表1-表6的一致性。输出所有不一致的精确单元格或时间/距离位置。
```

### 7.4 让模型专门审论文

```text
你是国赛 A 题匿名评委。请按“摘要、问题理解、假设、模型正确性、算法可信度、结果完整性、检验充分性、创新性、写作与规范”九个维度评审论文。优先找会导致结果不可信或直接扣分的问题。每条意见给严重度、证据位置、修复方案和预计耗时。最后只列出最值得在剩余 3 小时内修复的 10 项。
```

### 7.5 让模型写最终摘要

```text
请只使用论文和结果文件中已核验的数字写中文摘要，300-500 字。按“问题—统一模型—数值方法—四问关键定量结果—验证与贡献”组织。不得出现“效果较好、具有一定价值、提供参考”等空话；不得引入正文没有的数据；所有时间、浓度、误差与百分比必须与结果文件一致。
```

---

## 8. 最终验收清单

### 8.1 数学与物理

- [ ] 所有公式量纲一致，`T_K=T_°C+273.15`。
- [ ] 主模型使用圆柱径向散度，而非平板 Laplace 算子。
- [ ] `r=0` 无除零，中心通量为 0。
- [ ] 表面热/质通量符号与升温、干燥方向一致。
- [ ] 变系数位于散度内部。
- [ ] 问题3用全域最大含水率判定。
- [ ] 问题4说明材料坐标、半径插值和域外空值。
- [ ] 4 h 后环境边界方案有数据依据和敏感性比较。

### 8.2 数值与代码

- [ ] 至少三档空间网格完成收敛检查。
- [ ] 至少两档时间步或容差完成收敛检查。
- [ ] 温度和含水率无非物理振荡、负值或越界。
- [ ] 固定域与移动域质量收支残差已报告。
- [ ] 有独立解析/算法/二维模型交叉验证。
- [ ] 一条命令能重建 Excel、表格和图片。
- [ ] 代码中没有硬编码最终答案。

### 8.3 Excel

- [ ] result1 有“温度”“水分浓度”两表，时间 1-1800 s，空间步长 0.1 cm。
- [ ] result2 有“温度”“水分浓度”两表，时间 1-10800 s，空间步长 0.1 cm。
- [ ] result3 每 60 s 输出至结束，阈值判断使用未舍入数据。
- [ ] result4 每 60 s 输出至结束，表面列有效，域外固定位置为空。
- [ ] 四个文件均可正常打开，数字显示四位小数。
- [ ] 论文表1-表6全部由同一结果源自动导出。

### 8.4 论文

- [ ] 摘要含最终定量结果和验证误差。
- [ ] 每个假设都有依据及影响说明。
- [ ] 符号首次出现时定义，单位统一。
- [ ] 图题、表题、坐标、单位、图例完整。
- [ ] 正文不粘贴大段代码，只放算法流程或伪代码。
- [ ] 参考文献真实、可检索，且正文有对应引用。
- [ ] 模型优缺点具体，不写套话。
- [ ] PDF 无乱码、溢出、模糊图和跨页断表。

---

## 9. 风险登记表

| 风险 | 后果 | 预防与补救 |
|---|---|---|
| 4 h 后环境边界未说明 | 长时烘干时间不可复现 | 主方案 + 两个敏感性方案，报告时长差异 |
| 误把 °C 代入 Arrhenius 指数 | 扩散系数量级完全错误 | 单元测试并打印初始 D 数量级 |
| 用平均含水率判停 | 提前结束，违反“各处”要求 | 用全网格最大值和事件检测 |
| 问题4忽略移动域 | 第4问退化为简单换参数 | 固定域映射、移动边界守恒验证 |
| 固定物理点已超出收缩表面仍填数 | result4 出现无物理意义数据 | 域外留空，另设动态表面列 |
| 0.1 cm 网格直接作为计算网格 | 数值误差无法证明 | 细网格求解后插值输出 |
| 只展示漂亮云图，没有检验 | 可信度不足 | 收敛、守恒、交叉算法和敏感性齐全 |
| 手工复制论文表格 | 论文与附件不一致 | 自动生成表1-表6 |
| 过度追求二维模型 | 时间失控 | 一维为正式主线，二维仅做加分验证 |
| 最后才写论文 | 逻辑和图表断裂 | 从问题1起同步写方法、图注和校验结论 |

---

## 10. 冲奖重点排序

1. **先保证正确：** 单位、边界、圆柱几何、移动域、判停条件。
2. **再保证可信：** 收敛、守恒、独立校核、敏感性。
3. **再保证统一：** 四问共用模型与代码框架，结果和论文同源。
4. **最后做亮点：** 二维端部敏感性、移动边界机制解释、自动化复现。

真正能拉开差距的不是堆模型名称，而是让评委能沿着论文在较短时间内确认：方程来自物理、算法确实解对、附件严格满足要求、关键结论经得起扰动。
