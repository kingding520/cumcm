# A 题团队对接：新颖思路、真实结果原则与 MATLAB/论文接口

## 0. 先定纪律：任何创新都必须可计算、可复核

本题不需要为了“创新”强行增加题目未提供的参数。以下内容均只使用题目给定的边界、经验式、半径数据和数值解。**在 MATLAB 的正式运行、收敛检查和导出完成之前，论文中不写具体烘干时长、百分比差异、误差或“显著改善”等结论。**

建议把所有待证命题写成“分析指标”，由代码自动生成数值、图和表后再转写为结论。

---

## 0.1 不可上线门禁：针对常见致命缺陷的强制验收

以下不是“可选优化”，而是 MATLAB 程序、结果文件和 LaTeX 正文共同通过前不得发布的硬条件。

| 常见缺陷 | 必须改成什么 | 代码级验收 |
|---|---|---|
| 仅做指数衰减或经验拟合 | 圆柱径向非稳态热-质 PDE，中心对称与表面 Robin 边界 | `test_cylindrical_pde`：中心面通量为 0；升温时表面温度不低于中心；干燥时表面含水率不高于中心；初始 `Bi_h,Bi_m` 证明不能用集中参数模型 |
| 用平均含水率判定问题3结束 | 用全域最大值事件 `g(t)=max(C(:))-0.15` | `test_stop_event`：报告根 `g=0` 的插值时刻；导出的结束时刻满足未舍入 `max(C(:))<0.15`。题面用“低于”，论文写严格不等式，不把平均值或表面值替代为判据 |
| 问题4仅将 `R(t)` 代入固定域方程 | 固定参考域 `ξ=r/R(t)` 的守恒材料坐标，或严格 ALE | `test_moving_domain_reduction`：令 `R(t)=R0` 时，移动域程序应退化并复现固定域解；材料坐标中不得额外重复叠加收缩对流项 |
| 只有结果图，没有误差证据 | 简化对照、网格/时间收敛、离散质量收支 | `test_verification`：至少三档空间网格、两档时间精度；记录最大关键量差异和累计质量残差；未通过不得写“结果稳定” |
| 四问分别拼接模型 | 一个守恒型求解器，以配置切换题号、物性、边界和半径 | `test_unified_config`：四问均从同一 `run_all(cfg)` 调用；禁止复制粘贴四套 PDE 与手工修改结果 |

### 四条更隐蔽、同样必须拦截的错误

1. **长时边界未定义：** 附件1只有 4 h，问题3/4必须明确 `t>14400 s` 的恒温恒湿延拓；主方案和末点/末段均值方案均需实际运行比较。
2. **绝对温度错误：** `D` 中的 `exp(-3850/T)` 只能使用 `T_K=T_°C+273.15`；在单元测试中打印初始扩散系数量级，防止摄氏温度误入指数。
3. **变系数被错误展开：** 正式离散对象是 `∇·(D∇C)` 和 `∇·(k∇T)`；不能直接写为 `D∇²C` 或 `k∇²T` 后忽略系数梯度。
4. **收缩后仍输出域外数值：** 问题4某固定距离 `r>R(t)` 时该点已不存在，应导出空值；“药材表面”列由 `ξ=1` 的真实解单独输出，不得用最近内点冒充。

### 团队统一口径

> 我们不把“曲线平滑”当作结果正确。每个结论必须同时具备：守恒 PDE、正确边界与单位、全域阈值判停、移动边界退化检验、网格时间收敛、质量收支，以及从 MATLAB 唯一结果源自动生成的 Excel 与 LaTeX 图表。

---

## 1. 最值得做的五个新颖点

### 创新点 A：收缩效应的二维归因，而非只报“问题4变快/变慢”

问题3到问题4同时改变了两类因素：几何半径 `R(t)` 变化，且 `ρ,c_p,k,D` 的经验式也变化。直接比较两题，只能得到总差异，解释不了差异来源。

设置四组完全由已有模型构成的反事实计算：

| 编号 | 几何 | 物性 | 作用 |
|---|---|---|---|
| `M00` | 固定 `R=R0` | 附录3 | 问题3基准 |
| `M10` | 收缩 `R(t)` | 附录3 | 只识别几何收缩效应 |
| `M01` | 固定 `R=R0` | 附录4 | 只识别物性替换效应 |
| `M11` | 收缩 `R(t)` | 附录4 | 问题4完整模型 |

对烘干结束时间、中心含水率和表面含水率分别计算：

\[
\Delta_R=Y_{10}-Y_{00},\qquad
\Delta_P=Y_{01}-Y_{00},\qquad
\Delta_{RP}=Y_{11}-Y_{10}-Y_{01}+Y_{00}.
\]

其中 `ΔR` 是几何收缩的主效应，`ΔP` 是物性替换的主效应，`ΔRP` 是二者的非线性交互效应。`Y` 必须由正式程序实际输出，不能预设符号或大小。

这组计算的价值在于：它把第4问从“加一个动态半径”提升为**机制归因问题**，不增加任何编造参数。

**代码手交付：** `run_counterfactuals.m`、四组 `.mat` 文件、一张分组柱图和一张机制对比表。

**论文手写法：** 先给四组定义，再报告 `ΔR、ΔP、ΔRP` 的真实数值和方向；若交互项小，说明两种机制近似可加，若不小，说明必须采用完整耦合模型。

---

### 创新点 B：用“动态阻力分配”解释到底卡在内部扩散还是表面传质

题目给出了内部扩散系数 `D` 和表面传质系数 `h_m`。不能只画含水率云图，可以从数值解中构造无需新增参数的阻力指标。

设表面水分通量为

\[
J_s(t)=h_m\,[C_s(t)-C_\infty(t)],
\]

并定义

\[
R_{\mathrm{int}}(t)=\frac{C_c(t)-C_s(t)}{J_s(t)},\qquad
R_{\mathrm{surf}}=\frac{1}{h_m},\qquad
\eta_{\mathrm{int}}(t)=\frac{R_{\mathrm{int}}}{R_{\mathrm{int}}+R_{\mathrm{surf}}}.
\]

这里 `C_c` 和 `C_s` 分别为中心、表面含水率；量纲均可化为 `s/m`。`η_int` 越接近 1，内部扩散越是瓶颈；越接近 0，表面传质越是瓶颈。

**必须做的数值保护：** 若 `|J_s|` 很小，则该时刻阻力比不稳定，应设为 `NaN` 而非强行作图；图中只展示有效区间。

**可形成的结论：** 预热、主干燥、临近终止三阶段的主导阻力是否转移；收缩是否改变内部/表面阻力的占比。这是比“中心最慢干”更有解释力的工程语言。

---

### 创新点 C：干燥前沿与“安全结束”的空间证据

问题3要求“各处水分浓度低于 0.15 kg/kg”，不是只要报一个时间。令 `C*=0.15`，在每个时刻从表面向中心搜索 `C=C*` 的等值位置 `r_*(t)`；定义已达标干燥层厚度

\[
\delta(t)=R(t)-r_*(t).
\]

规则：

- 若表面尚未达到阈值，则 `δ=0`；
- 若全域达到阈值，则 `δ=R(t)`，并触发正式结束事件；
- 等值位置通过相邻网格线性插值取得，避免把网格点误判成精确前沿。

图形建议为 `δ/R` 随时间曲线，辅以 3-4 条真实径向剖面。它直接证明达标是从表面向中心推进，而不是抽象的平均含水率下降。

**注意：** 该指标是结果解释图，不替代正式终止条件；终止仍由未舍入的 `max_r C(r,t)<0.15` 决定。

---

### 创新点 D：用等效扩散时间解释收缩为什么值得建模

收缩的一个清晰几何机制是特征扩散长度缩短。对真实求解过程定义后处理指标

\[
\mathcal{F}(t)=\int_0^t \frac{\bar D(\tau)}{R(\tau)^2}\,d\tau,
\]

其中 `\bar D` 是当前截面上的体积加权平均扩散系数，由实际 `D(C,T)` 场计算，不预设常数。固定半径时将 `R(τ)=R0`。

`\mathcal F` 不是新的控制方程，也不用于替代原 PDE；它是用于解释“相同日历时间下，收缩模型累积了多少无量纲扩散进程”的诊断量。配合

\[
S_R(t)=\left[\frac{R_0}{R(t)}\right]^2
\]

可将收缩导致的几何加速单独可视化。

**论文亮点句式模板（待数值验证后使用）：** “半径收缩通过 `1/R^2` 放大无量纲扩散推进，且该效应与物性变化存在/不存在明显交互，因此固定几何近似会……”。省略号必须由 `M00-M11` 的真实结果补全。

---

### 创新点 E：从经验式直接推出“温度敏感还是含水敏感”

附录3的扩散系数满足

\[
\ln D=\ln(2.4\times10^{-3})-\frac{0.45}{C}-\frac{3850}{T_K}.
\]

故相对灵敏度（弹性）为

\[
E_C=\frac{\partial\ln D}{\partial\ln C}=\frac{0.45}{C},\qquad
E_T=\frac{\partial\ln D}{\partial\ln T_K}=\frac{3850}{T_K}.
\]

附录4只需把 `0.45` 改为 `0.30`。这些式子无需差分试验即可从题设经验式严格得到。

代码可在每个时刻输出截面平均、中心、表面三处的 `E_C,E_T`。这样能解释：温度升高和含水率变化究竟通过哪一项主要改变 `D`，以及该主导性是否随干燥阶段变化。

**边界：** 灵敏度分析只讨论经验式内的局部相对变化，不应夸大为“实际控制变量的最优策略”，因为题目没有开放优化决策或质量约束。

---

## 2. 三个高可信度加分项

### 2.1 两种独立验证，不拿“画得平滑”当验证

1. **简化解析/半解析交叉验证：** 固定 `D,k` 且常环境边界时，使用圆柱体 Fourier/Fick 特征函数级数或高精度参考网格，核验自编离散程序。
2. **二维轴对称敏感性：** 以 `(r,z)` 模型评估中截面径向剖面和结束时刻对端面散热/传质的影响。正式结果仍可用一维径向模型，因为题目输出为到中心的径向距离；二维只作为“一维近似误差量化”。

二维模型不是必做主线。它必须在一维结果和四个 Excel 文件稳定后开展，避免拖延交付。

### 2.2 边界延拓的不确定性带

附件1仅到 4 h，而问题3/4需持续到干燥结束。应把这一事实明写出来，并对三种无外部参数的延拓方案比较：

1. 恒定为物理设定值 `(50°C, 0.05 kg/kg)`；
2. 保持附件最后一个观测点；
3. 保持末 30 或 60 min 的平均值。

正文给主方案结果；补充表给三者的结束时间范围。若三者差别小，说明结论稳健；若差别大，说明后段边界是主要不确定性。不得悄悄选择对结果最有利的方案。

### 2.3 离散质量收支作为结果真实性凭据

对固定半径模型，记录每一步：

\[
\text{库存水分变化} + \text{表面外流积分} = \text{离散残差}.
\]

对收缩模型，在材料坐标下以相应 Jacobian/控制体体积计算库存。输出累计相对残差曲线和最大残差；该数据由程序自动写进 `validation_report`。这是最能证明代码没有“算得像，但其实边界写反”的检查之一。

---

## 3. 立即分派的 MATLAB 接口

### 3.1 代码手：唯一可信结果源

建议把正式运行统一为：

```matlab
cfg = make_config();
out = run_all(cfg);
audit = validate_all(out, cfg);
export_all(out, audit, cfg);
```

`out` 至少包含以下字段：

```matlab
out.q1.t; out.q1.r; out.q1.T; out.q1.C;
out.q2.t; out.q2.r; out.q2.T; out.q2.C;
out.q3.t; out.q3.r; out.q3.C; out.q3.t_end;
out.q4.t; out.q4.xi; out.q4.R; out.q4.C; out.q4.t_end;
out.meta.git_commit; out.meta.created_at; out.meta.config;
```

不得把论文数字、Excel 数字或图的数据单独手填。`export_all` 应从同一个 `out` 导出 `result1.xlsx-result4.xlsx`、论文 CSV 表和 MATLAB 图片。

### 3.2 论文手：只接受可追溯的“图表包”

代码手每次交付一个只读图表包：

```text
exports/
├─ tables/table1.csv ... table6.csv
├─ figures/fig01_boundary.png ...
├─ figures/figXX_counterfactual.png
├─ validation/mesh_convergence.csv
├─ validation/mass_balance.csv
├─ validation/run_manifest.txt
└─ result1.xlsx ... result4.xlsx
```

论文手只能从这里引用数值。每个图表旁写明：生成脚本、运行配置、数据文件、单位、四舍五入规则。改图或改数字必须回到 MATLAB 脚本，重新运行后再进入 LaTeX。

### 3.3 建模手：必须冻结的 10 个配置

代码开始前，在 `make_config.m` 中唯一维护：

1. 单位换算；
2. 初值与几何；
3. 附录2/3/4物性函数；
4. `h,h_m`；
5. 附件1插值方式；
6. 4 h 后边界延拓方案；
7. 附件2半径插值方式；
8. 空间网格与内部时间步/容差；
9. 结束阈值和事件定位精度；
10. 问题4中域外固定物理距离的空值规则。

凡是不在 `config` 里的参数，都不允许在求解器内部“悄悄写死”。

---

## 4. 给论文手的结构增量

在原有论文结构外，建议增加一个小节“机制诊断与模型可信度”，以如下顺序组织：

1. 初始 `Bi_h`、`Bi_m`：选择分布参数模型的必要性；
2. 时空场与干燥前沿：说明阈值由表面向中心推进；
3. 动态阻力分配：说明内扩散/表面传质的阶段性瓶颈；
4. `M00-M11` 归因：说明收缩和物性变化各自贡献；
5. 网格收敛、质量收支、边界延拓敏感性：说明结果可信区间。

这样“创新”始终服务于题目四问，而不是另起一个无关专题。

---

## 5. 24 小时内的最小可行推进顺序

| 顺序 | 责任人 | 真正完成的判据 |
|---|---|---|
| 1 | 建模手 | `config`、方程和边界符号经三人走读确认 |
| 2 | 代码手 | 问题1固定域能运行，中心/表面方向正确，简化算例通过 |
| 3 | 代码手 | 问题1 Excel 自动导出；论文手从 CSV 生成表1/2 |
| 4 | 代码手 | 问题2/3完成，事件检测和质量收支通过 |
| 5 | 建模手+代码手 | 移动域问题4与 `M00-M11` 反事实组完成 |
| 6 | 论文手 | 只用图表包完成“模型、结果、验证”三节初稿 |
| 7 | 全组 | 敏感性、收敛、Excel 模板和 LaTeX 交叉复核 |

如果时间紧，优先保证主线 `问题1-4 + 收敛 + 质量收支 + Excel + 论文`；动态阻力、前沿和二维归因按优先级加入。`M00-M11` 归因的投入产出比最高，应优先于二维模型。

---

## 6. 参考理论定位（供论文检索，不直接替代题目依据）

- 收缩会改变干燥过程中的热质传递和尺寸演化，建模中常见独立收缩处理、移动网格与固体坐标等路径：Katekawa 与 Silva 的综述，*Drying Technology*，DOI `10.1080/07373930500538519`。
- 干燥时的收缩、温湿耦合和材料结构变化被视为重要但具有挑战的机制，适合在本文中采用“题设经验半径 + 移动边界传递”的受约束形式，而非虚构力学子模型：[Zhang 等的综述](https://pubmed.ncbi.nlm.nih.gov/33350150/)。
- 近期移动边界干燥研究也使用 ALE/移动边界、温湿相关扩散率和表面阻力，但该题没有实验曲线用于反演参数，因此本文不做逆参数识别，只做题设参数下的正问题与敏感性分析：[相关研究](https://www.tandfonline.com/doi/full/10.1080/07373937.2026.2686407)。

这些文献仅用于说明方法选择的合理性；最终模型、参数和结果必须以题目与本队 MATLAB 实算为准。
